import { Metadata } from "next";
import { RegistrationVerificationPageWrapper } from "./wrapper";

export const metadata: Metadata = { title: "Verify account" };

export default RegistrationVerificationPageWrapper;
